// Token jwt;
export const TOKEN_COOKIE = "TOKEN"

// Refresh token jwt;
export const REFRESH_TOKEN_COOKIE = "RTOKEN"

// Sigla para identificar o cookie que guarda o horário da data de expiração.
export const JTE_COOKIE = "JTE"
